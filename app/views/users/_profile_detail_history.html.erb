<div class="profile-products-block">
  <div class="profile-products-inner-block">
    <% if current_user.orders.delivered.any? %>
      <% current_user.orders.delivered.order(:created_at => 'DESC').each do |order|
        @order = order   %>
        <%= render :partial => 'users/orders/profile_order_row' %>
      <% end %>
    <% else %>
      <div class="no-products-message">
        <h5>Todavía no realizaste o finalizaste ninguna compra</h5>
      </div>
    <% end %>
  </div>
</div>